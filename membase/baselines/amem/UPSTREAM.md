# A-MEM 实现来源与本地接入

基于 MemBase 内置 A-MEM，官方修复参考提交：
`WujiangXu/A-mem-sys@f303dfc71e07bdc787f4bc135d4cea328ae30e99`。

回补 `find_related_memories` 返回实际记忆标识，以及 `process_memory` 按该标识更新邻居的修复；不替换整套官方代码。保留现有分析与演化提示、最近 5 个邻居、查询排序、最终前 k 条规则及演化刷新阈值。即使实现收集了链接邻居，最终仍沿用原来的前 k 条截取，不宣称扩大了召回。

接入调整：共享有界请求与账本；缺少真实日期时可保留未知；集合按运行和样本隔离；刷新使用更新操作，跳过相同文档的重复嵌入；原子保存笔记及尚未刷新的索引状态。分析与演化的可恢复模型错误仍采用原有空属性/不演化回退，但写入警告；基础故障不被吞掉。

MAB 的公开输入优先级作为独立任务上下文附在原提示之后，不作为记忆笔记、问题或标准答案写入。比较时每组方法使用相同构建和回答模型，嵌入统一为 `text-embedding-3-small`，不称为原论文配置的逐项复现。

## 2026-09-08：按论文实验入口重新核对

A-mem-sys README 明确将论文复现指向 `WujiangXu/AgenticMemory`。本次固定其提交 `0c8039f28fdcc08189a23c07a3437d9d2482f9c2`：

- `test_advanced.py` 的 `evaluate_dataset` 和命令行 `--retrieve_k` 默认10；推荐的稳健入口同样默认10。系统库 `search_agentic` 的函数默认5不等于论文实验默认。当前继续使用10，不做正式题目上的参数扫描。
- `memory_layer.py` 分析/演化温度默认0.7，演化邻居5，刷新阈值100。当前对齐这些值。
- 论文仓库的OpenAI输出上限确实为1000；系统库对应路径则允许不指定。由于已观察到Qwen在1000处频繁截断，按用户要求保留“不指定输出上限”的明确例外，不宣称所有输出参数完全复现论文。
- 移除接入层额外的8000最终证据预算和16000输入预算：AMEMConfig中二者默认null。模型服务实际窗口仍然有效，超限应报错，不默默裁掉笔记。

参数溯源文件SHA-256：README.md `411288846bc456a3e7dbb00e6f879b46a559f3752835aed4023f580cdafbd36e`；test_advanced.py `7559e34cdd6a3993bf50bf4795342ec39bce91b1220d56ed206df021196c408a`；memory_layer.py `b9a5b5797881b25c5524b98a543b22a1bbbcc7f285437ca667a5673e7f293eba`。

未发现该仓库针对MAB和Qwen3-30B-A3B的专属推荐；LoCoMo的问题类别分支、回答选择和原论文评测器不能移植到MAB。当前保留MAB公开输入和回答评分协议。模型与嵌入统一、未知时间、隔离集合、刷新修复和检查点均为已披露接入差异；这仍是统一模型下的MemBase A-MEM对照，而非完整运行原论文仓库。

前一轮记录的1.0/null来自系统库默认，本轮不再把它当作论文实验推荐。输出完整性三例验证仍是当时1.0设置的历史记录；本轮只做离线验证，不新增付费请求。
