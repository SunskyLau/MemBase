# A-MEM 实现来源与本地接入

基于 MemBase 内置 A-MEM，官方修复参考提交：
`WujiangXu/A-mem-sys@f303dfc71e07bdc787f4bc135d4cea328ae30e99`。

回补 `find_related_memories` 返回实际记忆标识，以及 `process_memory` 按该标识更新邻居的修复；不替换整套官方代码。保留现有分析与演化提示、最近 5 个邻居、查询排序、最终前 k 条规则及演化刷新阈值。即使实现收集了链接邻居，最终仍沿用原来的前 k 条截取，不宣称扩大了召回。

接入调整：共享有界请求与账本；缺少真实日期时可保留未知；集合按运行和样本隔离；刷新使用更新操作，跳过相同文档的重复嵌入；原子保存笔记及尚未刷新的索引状态。分析与演化的可恢复模型错误仍采用原有空属性/不演化回退，但写入警告；基础故障不被吞掉。

MAB 的公开输入优先级作为独立任务上下文附在原提示之后，不作为记忆笔记、问题或标准答案写入。比较使用统一的 `gpt-4.1-mini` 和 `text-embedding-3-small`，不称为原论文配置的逐项复现。
