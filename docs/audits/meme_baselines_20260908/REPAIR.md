# MEME 三个基线的修复记录

范围：完整上下文、稠密原文检索、MD-flat。此前的审计报告保留为历史问题记录，不覆盖或改写其结论。

本次已处理：统一官方记忆基线回答提示；清理未生效的脚本参数；保留官方温度、输出上限、检索数量和工具轮数；同步旧本地入口；创建并固定三个独立环境；准备已校验的离线分词缓存。官方仓库提交 `0271ad85389a963cbc4892a36391f868ba4d18d1` 的文件未改动。

运行适配复用官方方法和样本调度，补上分阶段检查点（checkpoint）、单题回答/评分终态、输入与代码版本绑定。无效工具输出、回答截断和不完整评分结构不能当作正常成功；合法“不更新记忆”仍保留。失败题计零与模型真实答错分开，完整性检查仍使用原定分母。

费用改为按样本持久记录所有调用，含失败与重试；记录模型、用途、实际参数、用量、终止原因、输入输出及时间。汇总增加变化前正确率、任务/跳数、上下文长度、技术失败数和分用途成本。变化前精确召回采用官方确定性实现，变化后的评分定义与平凡通过过滤（trivial-pass filtering）保持不变。未知用量与实际金额保持未知，不伪造收费数据。

验证：18 项直接相关的测试通过；另一次三基线运行器贯通与续跑测试通过。三环境的依赖检查和固定种子的 CPU/分词验证通过；真实客户端库使用模拟传输验证了请求登记。独立检查者从 `/tmp` 原样执行三个默认干运行（dry run）命令，核对范围、模型、并发、目录和脚本执行权限均通过。没有运行整个测试套件，没有发起付费模型请求，也没有启动正式实验。

仍需真实运行确认：服务商模型可用性、接口稳定性、实际输出截断比例、技术失败比例和准确率。测试通过不是全量实验已经完成或保证不会发生故障。运行方式见 `examples/README_meme.md`；默认新运行名称与旧结果隔离。
