# 本地实验产物

运行入口和无密钥模板已迁入 `examples/`，请阅读 [统一实验流程](../examples/README_experiments.md)。OurMem 复用 MemBase 三阶段运行器；六个官方基线保留原生执行。

本目录保存各实验的配置记录、日志、记忆数据库、检索结果、回答及评分。旧运行目录和数据库保持原样，不迁移，不覆盖；新三阶段流程须使用新的 `RUN_ID`。本轮没有跨运行记忆缓存。

根目录 `.gitignore` 忽略本目录的新产物；已有的说明文档仅用来指向新入口，不包含可执行的实验编排。
