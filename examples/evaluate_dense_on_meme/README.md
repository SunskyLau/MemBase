# MEME · dense

新实验执行本目录的 `./run_gpt4o_mini.sh` 或 `./run_qwen3_30b.sh`，按名称分别选择构建与回答模型；评判和嵌入服务配置保持一致。脚本可从任意目录调用，末尾加 `--dry-run` 不调用模型。`envs/.env` 提供服务地址和密钥。模型、模式及编号在具名脚本顶部填写，其余配置沿用 `run.example.sh`。旧 `run.sh` 保留供已有实验续跑。

默认核心实验先跑两个无填充样本，再跑 filler32k 全部 100 个样本。主结果只取后者：494 个变化前问题、694 个变化后问题。默认构建与回答模型 gpt-4o-mini，评判模型 gpt-4o-2024-11-20；嵌入模型 text-embedding-3-small，检索数量为官方默认 5。

三种方法统一使用官方记忆基线的回答提示。其余保留官方默认：温度 0、回答最多 500 词元（tokens）；MD-flat 内部最多 2000 词元（tokens）、五轮工具调用；不自动扩大，也不截短输入。完整上下文方法更换回答提示是明确的对照适配，不声称逐项复现官方论文配置。

新结果：`experiments/meme_dense/runs/gpt-4o-mini_core_01_gpt/` 或 `qwen3-30b-a3b-instruct-2507_core_01_qwen/`。相同配置可续跑；新配置改运行名称。旧结果不移动、不覆盖。

完整运行说明、费用口径和故障边界见 [MEME 运行说明](../README_meme.md)。
